// Stub for packages that ship non-JS syntax (Flow, native) and are not
// meaningful in a Jest unit test environment. Returning an empty object
// satisfies any require() callers without running native or polyfill code.
module.exports = {};
